# Foundation

This folder receives imported shared layers.

- `foundation/core` comes from `master-core`
- `foundation/wp` comes from `wp-overlay`

Do not treat this folder as product truth.

Use the root adapter docs and local `specs/` as the app-local authority.